"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const tmp_promise_1 = __importDefault(require("tmp-promise"));
const Preserve = __importStar(require("@truffle/preserve"));
const fs_fixture_1 = require("./fs.fixture");
const preserve_1 = require("./utils/preserve");
const writeFile = async (fullPath, content) => {
    // ensure directory exists for file
    await fs_extra_1.default.ensureDir(path_1.default.dirname(fullPath));
    await fs_extra_1.default.promises.writeFile(fullPath, content);
};
describe("Recipe", () => {
    let workspace;
    beforeAll(async () => {
        workspace = await tmp_promise_1.default.dir();
    });
    afterAll(async () => {
        await workspace.cleanup();
    });
    for (const { name, files, targeted, expected, events } of fs_fixture_1.tests) {
        describe(`test: ${name}`, () => {
            beforeAll(async () => {
                for (const file of files) {
                    const fullPath = path_1.default.join(workspace.path, name, file.path);
                    await writeFile(fullPath, file.content);
                }
            });
            it("returns the correct target", async () => {
                const fullPath = path_1.default.join(workspace.path, name, targeted);
                const result = await preserve_1.preserve({ path: fullPath });
                const target = result["fs-target"];
                const stringified = await Preserve.Targets.stringify(target);
                expect(stringified).toEqual(expected);
            });
            it("emits the correct events", async () => {
                const fullPath = path_1.default.join(workspace.path, name, targeted);
                const emittedEvents = await preserve_1.preserveWithEvents({ path: fullPath });
                expect(emittedEvents).toEqual(events);
            });
        });
    }
});
//# sourceMappingURL=fs.test.js.map