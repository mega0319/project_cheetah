"use strict";
/**
 * @module @truffle/preserve-fs
 */ /** */
Object.defineProperty(exports, "__esModule", { value: true });
exports.Recipe = void 0;
const fs_1 = require("./fs");
class Recipe {
    constructor() {
        this.name = "@truffle/preserve-fs";
        this.inputLabels = ["path"];
        this.outputLabels = ["fs-target"];
    }
    async *execute(options) {
        var _a;
        const { controls } = options;
        const { update } = controls;
        yield* update({ message: "Loading target..." });
        const targetPathOptions = {
            controls: controls,
            path: options.inputs.path,
            verbose: (_a = options.settings.verbose) !== null && _a !== void 0 ? _a : true
        };
        const fsTarget = yield* fs_1.targetPath(targetPathOptions);
        return { "fs-target": fsTarget };
    }
}
exports.Recipe = Recipe;
//# sourceMappingURL=index.js.map