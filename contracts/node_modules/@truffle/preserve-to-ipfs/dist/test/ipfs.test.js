"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ipfs_http_client_1 = __importDefault(require("ipfs-http-client"));
const Preserve = __importStar(require("@truffle/preserve"));
const ipfs_fixture_1 = require("./ipfs.fixture");
const fetch_1 = require("./utils/fetch");
const preserve_1 = require("./utils/preserve");
const ganache_1 = __importDefault(require("ganache"));
jest.setTimeout(20000);
describe("preserve", () => {
    // Default IPFS nodes exposed by Ganache
    const address = "http://localhost:5001/api/v0";
    let ganacheServer;
    let ipfsClient;
    beforeAll(async () => {
        ganacheServer = ganache_1.default.server({
            flavor: "filecoin",
            miner: {
                instamine: "strict"
            },
            logging: {
                quiet: true
            }
        });
        await ganacheServer.provider.blockchain.waitForReady();
        await ganacheServer.listen(7777);
        ipfsClient = ipfs_http_client_1.default({ url: address });
    });
    afterAll(async () => {
        await (ganacheServer === null || ganacheServer === void 0 ? void 0 : ganacheServer.close());
    });
    for (const { name, target, events } of ipfs_fixture_1.tests) {
        // separate describe block for each test case
        describe(`test: ${name}`, () => {
            it("saves correctly to IPFS", async () => {
                const cid = await preserve_1.preserveToIpfs(target, address);
                const retrieved = await fetch_1.fetch({ cid, ipfs: ipfsClient });
                expect(await Preserve.Targets.stringify(retrieved)).toEqual(await Preserve.Targets.stringify(target));
            });
            it("emits the correct events", async () => {
                const emittedEvents = await preserve_1.preserveToIpfsWithEvents(target, address);
                expect(emittedEvents).toMatchObject(events);
            });
        });
    }
});
//# sourceMappingURL=ipfs.test.js.map